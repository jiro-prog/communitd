{
  "//": [
    "エディタ用・調査用の設定。全体を checkJs で見るので、`npx tsc -p jsconfig.json` で残りの件数が分かる。",
    "**CI の型検査は tsconfig.check.json** (`// @ts-check` を付けたファイルだけを見る)。",
    "エディタ (VS Code) はこのファイルを見て JSDoc から推論するので、置いておくだけでも",
    "補完と型の食い違いの指摘が効く。"
  ],
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2023"],
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": false,
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*.js", "scripts/**/*.mjs", "test/**/*.js"],
  "exclude": ["node_modules", "data", "tmp", "sandbox", ".worktrees"]
}
